

# Add Google Analytics (GA4)

Add the Google tag to `index.html` immediately after the `<head>` element. Since this is a single-page app, one placement covers all routes.

## File Modified (1)
| File | Change |
|------|--------|
| `index.html` | Add GA4 gtag.js snippet (`G-JZ23BREHEL`) right after `<head>` |

