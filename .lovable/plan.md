

## Update Favicon

1. Copy `user-uploads://image-11.png` to `public/favicon.png`
2. Update `index.html` to reference the new favicon:
   - Add `<link rel="icon" href="/favicon.png" type="image/png">` in the `<head>`

