

# Increase BBB Logo Size

Two small changes:

1. **`src/pages/Index.tsx`** — Change the BBB logo in the trust strip from `h-6` to `h-8`
2. **`src/components/layout/Footer.tsx`** — Change the BBB logo from `h-8` to `h-10`

